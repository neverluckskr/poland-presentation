# 🚀 Інструкція з деплою на Netlify

## Крок 1: Підготовка проекту для GitHub

1. Переконайтеся, що всі зміни закомічені:
   ```bash
   git add .
   git commit -m "Prepare for Netlify deployment"
   ```

2. Створіть репозиторій на GitHub (якщо ще не створено):
   - Перейдіть на https://github.com
   - Створіть новий репозиторій
   - Назвіть його, наприклад: `poland-presentation`

3. Завантажте код на GitHub:
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/poland-presentation.git
   git branch -M main
   git push -u origin main
   ```

## Крок 2: Деploy на Netlify

### Варіант A: Через веб-інтерфейс Netlify

1. Перейдіть на https://app.netlify.com
2. Увійдіть або зареєструйтеся
3. Натисніть "Add new site" → "Import an existing project"
4. Виберіть "GitHub" та авторизуйтеся
5. Виберіть репозиторій `poland-presentation`
6. Netlify автоматично визначить налаштування з `netlify.toml`:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
7. Натисніть "Deploy site"

### Варіант B: Через Netlify CLI

1. Встановіть Netlify CLI:
   ```bash
   npm install -g netlify-cli
   ```

2. Увійдіть:
   ```bash
   netlify login
   ```

3. Запустіть деплой:
   ```bash
   netlify deploy --prod
   ```

## Важливо! 

### Переконайтеся, що папка `public/assets` існує

Перед деплоєм переконайтеся, що папка `public/assets` містить всі зображення:

```bash
# Windows PowerShell
xcopy /E /I /Y assets public\assets

# Linux/Mac
cp -r assets public/assets
```

Або вручну скопіюйте папку `assets` в `public/assets`.

## Налаштування автоматичного деплою

Після першого деплою, Netlify автоматично деплоїть нові зміни при кожному push в GitHub.

## Перевірка після деплою

1. Перевірте, що сайт відкривається
2. Перевірте, що всі зображення завантажуються
3. Перевірте мобільну версію

## Можливі проблеми

### Зображення не завантажуються
- Переконайтеся, що папка `public/assets` існує та містить файли
- Перевірте шляхи до зображень в коді (мають починатися з `/assets/`)

### Помилка при білді
- Перевірте, що всі залежності встановлені: `npm install`
- Перевірте, що Node.js версія 18 або новіша

## Оновлення сайту

Щоб оновити сайт, просто зробіть:
```bash
git add .
git commit -m "Update presentation"
git push
```

Netlify автоматично задеплоїть нову версію!

