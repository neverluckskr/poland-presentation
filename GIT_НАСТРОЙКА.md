# 🔧 Настройка Git для проекта

## Шаг 1: Настройка имени и email

Git требует знать, кто вы. Выполните эти команды (замените на свои данные):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

### Пример:
```bash
git config --global user.name "Влад Шегеда"
git config --global user.email "vladv@example.com"
```

**Важно:** 
- `--global` означает, что эти настройки будут применяться ко ВСЕМ вашим git-проектам
- Если хотите настроить только для этого проекта, уберите `--global`:
  ```bash
  git config user.name "Ваше Имя"
  git config user.email "ваш-email@example.com"
  ```

## Шаг 2: Проверка настроек

После настройки проверьте, что всё правильно:

```bash
git config --global user.name
git config --global user.email
```

Должны показаться ваши имя и email.

## Шаг 3: Создание коммита

Теперь можно создать коммит:

```bash
git commit -m "Initial commit: Poland presentation with mobile support"
```

## Шаг 4: Добавление удаленного репозитория

После создания коммита нужно добавить удаленный репозиторий GitHub:

1. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com
   - Нажмите "New" (или "+" → "New repository")
   - Назовите репозиторий, например: `poland-presentation`
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
   - Нажмите "Create repository"

2. **Подключите GitHub к вашему проекту:**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/poland-presentation.git
   ```
   
   Замените `YOUR_USERNAME` на ваш GitHub username!

3. **Загрузите код на GitHub:**
   ```bash
   git branch -M main
   git push -u origin main
   ```

## ⚠️ О предупреждениях про LF/CRLF

Предупреждения `LF will be replaced by CRLF` - это нормально. Это просто означает, что Git автоматически конвертирует окончания строк для Windows. Их можно игнорировать.

## 📋 Полная последовательность команд

```bash
# 1. Настройка Git (один раз)
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"

# 2. Инициализация (уже сделано)
git init

# 3. Добавление файлов (уже сделано)
git add .

# 4. Создание коммита
git commit -m "Initial commit: Poland presentation with mobile support"

# 5. Добавление удаленного репозитория (после создания на GitHub)
git remote add origin https://github.com/YOUR_USERNAME/poland-presentation.git

# 6. Переименование ветки в main
git branch -M main

# 7. Загрузка на GitHub
git push -u origin main
```

## 🔐 Если потребуется авторизация

При первом `git push` GitHub может попросить авторизацию:
- **Если используете HTTPS**: Введите ваш GitHub username и Personal Access Token (не пароль!)
- **Как создать Token**: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token

## ✅ Готово!

После успешного `git push` ваш код будет на GitHub, и вы сможете задеплоить его на Netlify!

